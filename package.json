{
  "name": "campus-water-forest-ice5week",
  "version": "0.6.0",
  "private": true,
  "description": "Campus Reef — ICE Week 5 campus sustainability game prototype",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test --test-reporter=spec tests/core.test.cjs",
    "test:browser": "node --test --test-reporter=spec tests/browser.test.cjs",
    "build": "node scripts/build.cjs",
    "start": "node scripts/serve.cjs"
  }
}
